{
  "name": "i-want-to-party",
  "version": "1.0.0",
  "description": "Tibia party seeker",
  "main": "index.js",
  "author": "Wojciech Ossowski",
  "license": "MIT",
  "private": true,
  "scripts": {
    "postinstall": "(cd client && yarn install) && (yarn bundle)",
    "test": "yarn jest",
    "build": "yarn webpack --mode=development --config ./webpack/build.backend.config.js",
    "build:backend": "yarn webpack --config ./webpack/build.backend.config.js",
    "build:backend:watch": "yarn webpack --mode=development --config ./webpack/build.backend.config.js --watch",
    "build:client": "(cd client && yarn build)",
    "bundle": "yarn build:backend && yarn build:client",
    "start": "node backend_bundle.js",
    "start:environment": "$(cd docker && docker-compose up -d > /dev/null)",
    "stop:environment": "$(cd docker && docker-compose down > /dev/null)",
    "postgres:start": "$(cd docker && docker-compose up -d tibia_database)",
    "postgres:rm": "$(cd docker && docker-compose rm -f tibia_database > /dev/null)",
    "postgres:stop": "$(cd docker && docker-compose stop tibia_database > /dev/null)",
    "postgres:restart": "yarn postgres:stop && yarn postgres:rm && yarn postgres:start",
    "postgres:setup": "yarn knex migrate:latest && yarn knex seed:run",
    "knex": "./node_modules/.bin/knex --knexfile ./backend/src/common/infrastructure/repositories/postgres/knexfile.js"
  },
  "dependencies": {
    "axios": "^0.19.1",
    "bluebird": "^3.7.2",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "ioredis": "^4.14.1",
    "knex": "^0.20.8",
    "morgan": "^1.9.1",
    "pg": "^7.17.1"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/bluebird": "^3.5.29",
    "@types/cors": "^2.8.6",
    "@types/express": "^4.17.2",
    "@types/ioredis": "^4.14.4",
    "@types/jest": "^24.9.0",
    "@types/node": "^13.1.7",
    "awesome-typescript-loader": "^5.2.1",
    "jest": "^24.9.0",
    "ts-jest": "^24.3.0",
    "typescript": "^3.7.5",
    "webpack": "4.41.2",
    "webpack-cli": "^3.3.10",
    "webpack-node-externals": "^1.7.2"
  },
  "engines": {
    "node": "12.x"
  }
}
